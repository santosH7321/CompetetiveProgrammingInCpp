#include <iostream>
using namespace std;
int main()
{
    float height, width, AreaOfRectangle;
    cout << "Enter height and width: ";
    cin >> height >> width;
    AreaOfRectangle = (height + width) / 2;
    cout << "Area of Rectangle is: " << AreaOfRectangle;
}
