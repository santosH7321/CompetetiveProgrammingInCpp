#include<iostream>
using namespace std;
int main(){

    //Initilize the datatype and Variable Where int is a data type and a and b is variable 
    int a, b;

    //where cin is taking Input to user like in c (scanf)and this is >> (Extraction Operator)
    cin>>a>>b;
    cout<<"Your Input Number is: "<<a <<"&"<<b <<endl;
}